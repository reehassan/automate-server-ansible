# === General ===
*.log
*.tmp
*.bak
*.swp
*.swo
.DS_Store
Thumbs.db

# === Python (if using) ===
__pycache__/
*.pyc

# === VSCode/JetBrains/Editor ===
.vscode/
.idea/

# === Ansible ===
*.retry
*.tar.gz
*.zip
vault_pass.txt   # If you store Ansible Vault password in plaintext (not recommended)

# === Terraform ===
*.tfstate
*.tfstate.backup
.terraform/
terraform.tfvars
terraform.tfvars.json
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# === Git itself (already ignored by default, but safe to list) ===
.git/

# === Secrets (Always make sure no hardcoded secrets!) ===
secrets.yml
private_key.pem
*.key
*.pem
*.crt

# === OS Specific ===
# macOS
.AppleDouble
.LSOverride
Icon?

# Linux
*~

# Windows
ehthumbs.db
Desktop.ini
